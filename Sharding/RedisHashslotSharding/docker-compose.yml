version: '3.8'

services:
  redis:
    build:
      context: ./RedisHashslotSharding
      dockerfile: Dockerfile
    container_name: redis-hashslot-sharding-app
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:5000
    ports:
      - "5000:5000"
